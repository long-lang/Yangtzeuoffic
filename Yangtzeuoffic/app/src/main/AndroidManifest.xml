<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sweethearts">

    <uses-permission android:name="android.permission.INTERNET" /> <!-- SD卡写入权限 写入扩展存储，向扩展卡写入数据 -->
    <uses-permission android:name="android.permission.VIBRATE" /> <!-- 添加写文件权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.FullScreen"
        android:usesCleartextTraffic="true">

        <activity android:name=".view.Activity.LaucherActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.Activity.MainActivity"></activity>
        <activity android:name=".view.Activity.LoginActivity"></activity>
    </application>

</manifest>